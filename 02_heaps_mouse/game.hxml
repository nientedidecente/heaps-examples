# linking heaps library
-lib heaps
# linking the hldx (DirectX) library (works only under Windows)
# on other OS you can use hlsdl to link SDL
-lib hldx
# Define the main class of the application with an entry point
-main Game
# Output file
-hl game.hl